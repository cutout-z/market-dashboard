services:
  htf_worker:
    build: .
    restart: unless-stopped
    command: python worker_htf.py --interval 604800
    environment:
      - FRED_API_KEY=${FRED_API_KEY}
      - FMP_API_KEY=${FMP_API_KEY}
    volumes:
      - .:/app
    depends_on:
      - dashboard
